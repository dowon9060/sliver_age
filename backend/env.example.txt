# 이 파일을 .env로 복사해서 사용하세요
# cp env.example.txt .env

# 데이터베이스
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your_password_here
DB_DATABASE=silver_age

# Redis (선택사항 - 실시간 기능용)
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT 인증
JWT_SECRET=change-this-to-a-random-secure-string-in-production
JWT_EXPIRES_IN=7d

# AWS S3 (이미지 업로드용 - 선택사항)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_S3_BUCKET_NAME=silver-age-uploads
AWS_REGION=ap-northeast-2

# Firebase FCM (푸시 알림용 - 선택사항)
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_PRIVATE_KEY=your-firebase-private-key
FIREBASE_CLIENT_EMAIL=your-firebase-client-email

# 네이버 맵 API (선택사항)
NAVER_MAP_CLIENT_ID=your-naver-map-client-id
NAVER_MAP_CLIENT_SECRET=your-naver-map-client-secret

# 토스페이먼츠 (테스트 키 또는 실제 키)
# 테스트 키는 https://docs.tosspayments.com/ 에서 확인
TOSS_PAYMENTS_SECRET_KEY=test_sk_
TOSS_PAYMENTS_CLIENT_KEY=test_ck_

# 앱 설정
PORT=3000
NODE_ENV=development

